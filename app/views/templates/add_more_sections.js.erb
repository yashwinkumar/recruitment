var section_html = ("<%= escape_javascript render partial: '/templates/sections_form' %>");

$('.add_more_sections').append(section_html);

$('.remove_j_qtn').on('click',function (e) {
  e.preventDefault();
  var sid = $(this).data('sid');
  $('#template_sections_attributes_' + sid + '__destroy').val(1);
  $('#section_field_' + sid).remove();
  $(this).remove();
  return false;
});